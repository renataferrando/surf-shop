import { Close, User, Cart, Search, ArrowLeft, Menu } from "./svg";

const mapper = {
  cart: <Cart />,
  close: <Close />,
  search: <Search />,
  user: <User />,
  arrowLeft: <ArrowLeft />,
  menu: <Menu />,
};

export default mapper;
